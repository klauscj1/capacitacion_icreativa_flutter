import 'package:flutter/material.dart';

class CalculadoraLina extends StatelessWidget {
  @override
  Widget build(Object context) {
    return Scaffold(
        appBar: AppBar(
      title: Text('Calculadora Lina'),
    ));
  }
}
