import 'package:flutter/material.dart';

class FormularioClaus extends StatelessWidget {
  const FormularioClaus({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Formulario claus'),
    );
  }
}
